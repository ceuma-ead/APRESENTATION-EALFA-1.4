// import { annotate } from 'https://unpkg.com/rough-notation?module';

// // Função para aplicar a anotação em todos os elementos de uma classe
// function aplicarAnotacao(classe, configuracao) {
//     const elementos = document.querySelectorAll(`.${classe}`);
    
//     // Itera sobre todos os elementos que possuem a classe fornecida
//     elementos.forEach((elemento) => {
//         const annotation = annotate(elemento, configuracao);
//         annotation.show();
//     });
// }

// aplicarAnotacao('rough-marcacao-restaurante', { type: 'box', color: '#f59a3d' });
// aplicarAnotacao('rough-sintese', { type: 'box', color: '#f59a3d' });
